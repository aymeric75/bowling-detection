# Model artifact

Place the Edge TPU-compiled detector here as `detect_edgetpu.tflite`.

The model originally committed with this project is not included because the
recovered files are truncated and cannot be loaded as TensorFlow Lite models.
The original detector had one class, `topPin`; its label map is preserved in
`labels.txt`.
